/* ---------------------------------------------------------------
   Supabase Dashboard → Project Settings → API
     SUPABASE_URL      = Project URL      (https://xxxx.supabase.co)
     SUPABASE_ANON_KEY = anon / publishable kulcs
   A Google Client secret NEM ide való: az kizárólag a Supabase
   felületén, az Authentication → Providers → Google alatt kerül be.
   Amíg ezek üresek, az alkalmazás bemutató módban indul.
   --------------------------------------------------------------- */

window.APP_CONFIG = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: ""
};
